import { MentionDirectiveDirective } from './mention-directive.directive';

describe('MentionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MentionDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
